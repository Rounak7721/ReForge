# dependencies
node_modules/
.pnp
.pnp.js

# next.js
/.next/
/out/
/build/
/dist/

# env — everything except the committed template
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# editor
.idea/
*.swp

# playwright mcp scratch output
.playwright-mcp/

# Stray Playwright screenshots (the MCP server writes to cwd unless given a path)
/*.png

# Logo-creator working directory: iterations and the HTML preview harness.
# The approved mark lives in components/marketing/wordmark.tsx and app/icon.svg.
/.skill-archive/

# editor state — .vscode/ here holds a local browse database, not shared config
/.vscode/
# screenshots captured by the playwright MCP server during QA
/.playwright-mcp/
